---
- name: Install DevOps Tools
  hosts: all
  become: yes
  vars:
    minikube_version: "latest"
    helm_version: "latest"

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install dependencies
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg
          - lsb-release
          - conntrack
        state: present

    - name: Install Docker
      apt:
        name: docker.io
        state: present

    - name: Ensure Docker is started and enabled
      systemd:
        name: docker
        state: started
        enabled: yes
      when: not ansible_check_mode

    - name: Add user to docker group
      user:
        name: root
        groups: docker
        append: yes
      when: not ansible_check_mode

    - name: Download Kubectl
      get_url:
        url: "https://dl.k8s.io/release/{{ lookup('pipe', 'curl -L -s https://dl.k8s.io/release/stable.txt') }}/bin/linux/amd64/kubectl"
        dest: /usr/local/bin/kubectl
        mode: '0755'

    - name: Download Minikube
      get_url:
        url: https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
        dest: /usr/local/bin/minikube
        mode: '0755'

    - name: Install Helm
      shell: |
        curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
      args:
        creates: /usr/local/bin/helm

    - name: Install networking tools
      apt:
        name:
          - socat
          - ufw
        state: present

    - name: Clean up networking (Nginx/IPTables)
      shell: |
        systemctl stop nginx || true
        # Clean nat rules
        iptables -t nat -F || true
        iptables -F || true
      ignore_errors: yes

    - name: Start Minikube (as root user)
      become: yes
      become_user: root
      shell: |
        minikube start --driver=docker --listen-address=0.0.0.0 --force \
          --extra-config=apiserver.service-node-port-range=8000-9000
      args:
        creates: /root/.minikube/machines/minikube/config.json
      register: minikube_start_result
      ignore_errors: yes
      when: not ansible_check_mode

    - name: Wait for CoreDNS to be ready
      become: yes
      shell: |
        kubectl wait --for=condition=ready pod -l k8s-app=kube-dns -n kube-system --timeout=180s
      when: not ansible_check_mode
      register: wait_result
      ignore_errors: yes

    - name: Show Minikube status
      become: yes
      shell: minikube status
      when: not ansible_check_mode
      ignore_errors: yes
