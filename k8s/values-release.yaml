replicaCount: 2
image:
  repository: DOCKER_USERNAME/devops-exam-app
  tag: 1.0.0
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 8000

serviceAccount:
  create: false
  name: ""

ingress:
  enabled: false

autoscaling:
  enabled: false

httpRoute:
  enabled: false
